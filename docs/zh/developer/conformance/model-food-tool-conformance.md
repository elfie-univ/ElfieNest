# Model、Food 与工具实现一致性台账

> 这是已拆解的模型、Food、工具实现与
> [模型、Food 与工具行为契约](../contracts/model-food-tool-behavior)的临时实现差距台账，只记录
> 当前未达标项；目标所有权只由[系统架构契约](../contracts/system)定义。

## 状态规则

- `open`：契约尚未实现，或当前实现存在安全问题。
- `in progress`：已有本地实现，但验收条件还没有全部通过。
- `closed`：代码、局部测试、架构测试和端到端契约全部通过。
- 除非设计契约明确要求兼容，否则增加兼容分支不能算作修复。

## 阻塞差距

| ID | 严重度 | 状态 | 差距 | 验收条件 |
| --- | --- | --- | --- | --- |
| AR-001 | P0 | closed | `configs/providers.yaml` 同时存在新版连接写入器和旧版配置写入器，修改无关系统或工具设置会清空连接。 | 文件只有一个类型模型和一个写入所有者；系统与工具设置不能改动 Provider 连接内容；回归测试能够复现并阻止数据丢失。 |
| AR-002 | P0 | closed | Provider 与模型验证已经进入粮食生成共用的模型证据投影。 | Provider 模型清单和 SQLite 最新观测组成唯一派生证据；粮食生成无需重启、复制 YAML 或维护第二配置源。 |
| AR-003 | P1 | closed | 粮食写入现在要求最近验证证据以及有效、可见的连接/模型引用。 | 每个落盘角色都能解析到启用连接下真实、可见的 endpoint 模型；没有候选时必须留空并给出警告。 |
| AR-004 | P1 | closed | 粮食领域已改为永久保底粮、常用粮和管理员自建粮食。 | 新数据根第一行初始化保底粮、第二行初始化常用粮；两份系统粮食永久可见且不可删除；其他粮食是普通自定义记录。 |
| AR-005 | P1 | closed | 粮食已有独立的启停、归档和受保护删除语义。 | 启停、归档/恢复、删除是三个独立命令；系统粮食不能归档/删除；自定义粮食只有已归档且无引用时才能删除。 |
| AR-006 | P1 | closed | 主粮解析现在严格使用已分配/默认粮、全局保底粮和明确的 `no_available_food` 结果。 | 解析严格按“精灵主粮、全局保底粮、明确不可用”执行，不会静默选择第一份粮食或旧配置。 |
| AR-007 | P1 | closed | 粮食可见性和精灵编辑器已收敛为粮食行上的一个主粮选择与可见性字段。 | 保底粮和常用粮对所有用户可见；只有自定义粮食配置用户权限；精灵页面只有一个主粮字段，候选是除保底粮外所有可见、启用且健康的粮食。 |
| AR-008 | P1 | closed | 粮食目录和可见性已经收口到 `nest.db.food_packages` 单一事实源；旧精灵级 `food_policy.yaml` 读取和固定接口已删除。 | 数据库粮食目录与 Nest 主粮分配是唯一事实源；缺失旧文件不会触发回退读取或写入。 |
| AR-009 | P1 | closed | Brain 记忆和结构化生成现在通过注入的 FoodPort/runtime 边界请求语义粮食角色。 | Brain 只请求 `primary`、`reasoning` 等语义角色；注入的 `FoodPort` 直接返回精灵有效套餐；结构化生成不经过 App Orchestration 代理即可使用所选角色。 |
| AR-010 | P1 | closed | 普通和结构化 Runtime 路径现在共用注入的安全 ToolPort 循环。 | 模型、Food、工具端到端契约证明精灵可以调用获准的安全工具、取得有界结果并继续生成。 |
| AR-011 | P1 | closed | 安全工具结果在重新注入模型前会裁剪，并记录截断证据。 | 每种工具有字节/条目限制，最终结果信封再次裁剪并记录截断信息，不能无限增长上下文。 |
| AR-012 | P1 | closed | 模型刷新按 endpoint ID 无损合并并保留手工记录，删除前检查粮食引用。 | 发现结果按 endpoint ID 合并并保留手工记录；消失的发现模型标记不可用；有粮食引用时拒绝破坏性删除。 |
| AR-013 | P1 | closed | Provider 管理和 Runtime 路由已统一使用基于连接的模型与语义粮食解析器；旧写入器和路由字段已清除。 | 最终只保留一套连接 API 和粮食解析器；旧写入器和旧路由事实源直接删除，不作为隐式回退。 |
| AR-014 | P1 | closed | 模型、Food、工具观测已统一进入专用追加式报告数据库和类型化存储 Port。 | 所有观测追加到 `reports/ai-runtime.sqlite`；当前/历史时点/run 查询通过；导出只写不读；架构测试拒绝旧事实路径和根级收据。 |
| AR-015 | P2 | closed | Provider 卡片、生命周期动作和统一添加流程已符合约定 UI；内置品牌资源从本地 public 资源树正确加载。 | 卡片固定为“模型、验证、修改、更多”；生命周期动作在“更多”中；唯一搜索选择器同时包含快捷、分类和自定义产品；本地资源全部可加载。 |
| AR-016 | P2 | closed | Provider 与模型目录发现已实现“官方、ElfieNest 远程、安装包本地、手工”的来源链，并且刷新不破坏记录。 | 目录描述真实产品与认证；发现严格按四级优先级执行、保留手工记录，并且验证通过前不能进入粮食。 |
| AR-017 | P2 | closed | 工具广告只包含已实现的安全搜索和工作区读取能力；延期的代码与 Skill 修改均不存在。 | 一期默认只暴露已实现的安全工具；延期工具和 Skill 修改关闭且不会提供给模型；可变或持久个人 Skill 状态没有活动事实源。 |
| AR-018 | P1 | in progress | 干净数据目录的语义、架构、全后端/前端和质量门已通过；剩余验收材料是 Provider、模型矩阵、粮食与精灵流程的可复查浏览器视觉证据。 | 使用干净临时 `ELFIE_HOME` 跑通契约全部 13 步，包括发现优先级、对比快照、生命周期保护和 `no_available_food`；Provider、模型矩阵、粮食与精灵浏览器验收应保留可复查的视觉证据；并且完整质量门必须通过。 |
| AR-019 | P1 | closed | 全部验证和矩阵查询现在使用追加式 report run，支持当前、历史时点和指定 run 投影。 | 有并发上限的全部验证写入一个完整 run；单项验证追加一个对象观测；当前、历史时点和指定 run 查询都保留各自测量时间。 |
| AR-020 | P1 | closed | 粮食页面和规划器已实现永久行顺序、按连接范围生成、最近验证门、五角色表和动态健康。 | 浏览器与 API 测试证明保底/常用顺序、范围内本地优先生成、diff/手工/保存流程、Primary/Reasoning/Vision/Tool/Fallback 绑定和证据派生健康，并且粮食不保存模型能力字段。 |

## 当前实现地图

1.5 版行为契约在拆包期间保留已经接受的语义；系统契约负责拆解目标。当前代码在五个
相互关联的区域存在差距：

| 区域 | 当前实现 | 与契约的差距 | 问题编号 |
| --- | --- | --- | --- |
| 持久化与报告 | `providers.yaml` 只有连接事实写入器；Provider/模型结果与 Food 证据都通过类型化存储 Port 从追加式报告数据库投影。 | 每份配置只能有一个类型化所有者；报告必须追加到 `reports/ai-runtime.sqlite`，并支持当前、历史时点和指定 run 投影。 | AR-001、AR-008 |
| Provider 与模型清单 | 已有可读连接 ID 和同产品多账号，刷新已无损合并、保护模型删除，并通过报告投影验证/矩阵；四级发现链和已配置卡片/统一添加流程也已落地。 | 必须实现官方/远程/内置/手工四级发现、无损合并、验证准入和完整/局部跨连接矩阵。 | 无 |
| 粮食领域与 Owner 页面 | `nest.db.food_packages` 保存永久系统行和自定义粮，包含五个简单角色绑定、启停/归档、受保护删除和证据派生健康。 | 永久保底粮/常用粮、自定义粮、简单角色到模型绑定、最近证据过滤、范围生成和证据派生健康已经实现。 | AR-004、AR-005、AR-020 |
| 权限与精灵路由 | 可见性是粮食行上的 `global`/`users` 字段；旧粮食 YAML 与 coarse/focus 路由已删除。 | 精灵页只有一个主粮字段；Elfie Resolver 必须按角色、一个可选套餐 Fallback、全局保底和 `no_available_food` 解析。 | AR-007、AR-008 |
| Tools 与验收 | 普通和结构化粮食执行共用注入的安全工具循环；工作区读取有界并记录截断；代码和技能修改保持关闭。 | 安全工具必须进入真实皮层路径，绑定精灵工作区并裁剪；延期工具保持关闭；13 步验收必须全部通过。 | AR-018 |

## 迁移分组

这些分组表达依赖顺序，不授权合入半成品阶段。每个合并的迁移切片都必须保持产品可运行，
并通过自己的聚焦验收门，后续切片才能依赖它。

实施遵循“先建立新事实源、再切换全部调用方、最后删除旧实现”。新旧代码可以在一个
尚未合并的切片中短暂共存，但同一事实只能有一个活动写入者。若旧读取器需要跨合并切片
保留，必须有明确缺口条目与删除门，不得产生新数据；替代链路通过后立即删除，不能成为
永久兼容层。

### 阶段一：单一事实源与报告数据库

范围：

- 让 v2 Provider 连接成为 `providers.yaml` 唯一 Schema，先切换调用方，再删除旧
  bundle 写入器；旧 Provider 事实接口只有在调用方全部切换后才能删除；
- 新增 `reports/ai-runtime.sqlite`、显式 Schema 初始化、WAL、report run、不可变
  observation 和当前/历史时点/指定 run 查询；
- 用 Report Repository 和派生证据查询替换 YAML Provider/模型报告与
  `model_evidence.yaml`；
- 按契约整理 `runtime/runtime.json`、locks 和路径帮助器；
- 删除旧根级报告的事实读取；精灵级 `food_policy.yaml` 从本阶段起禁止新增写入，
  旧读取只临时保留到阶段四替代链路通过验收。

验收门：

- 修改 Runtime 或 Tool 设置时，Provider 和粮食文件字节完全不变；
- 单项、局部批量、完整批量和历史时点报告测试通过；
- 架构测试拒绝已删除写入器、旧报告事实路径和任何新增 `food_policy.yaml` 写入。

关闭或推进：AR-001、AR-002、AR-008、AR-013、AR-014。

### 阶段二：Provider 清单、发现与模型矩阵

范围：

- 实现 `official`、可选 `remote_catalog`、`bundled_catalog`、`manual` 四级发现和
  无损合并；
- 远程服务器尚不存在时先建立可配置 Client 边界，当前保证官方与安装包内置路径可用；
- 增加连接启停、归档/恢复、受保护删除，以及模型隐藏/退役/引用保护；
- 验证结果写入 report run，只有最近验证通过的模型可以进入粮食；
- 完成连接卡片动作、统一搜索产品选择器、正确本地品牌资源、当前连接模型页和
  “全部验证”矩阵。

验收门：

- 同产品两个账号以可读不可变 ID 共存；
- 四级发现回退全部有测试，刷新不丢手工模型；
- 单项验证只改变一个矩阵对象，“全部验证”生成一个完整 run；
- 浏览器测试验证 `[模型] [验证] [修改] [更多]` 和统一添加流程。

关闭或推进：AR-003、AR-012、AR-015、AR-016、AR-019。

### 阶段三：粮食领域、生命周期与 Owner 流程

范围：

- 用稳定套餐替换固定粮食和 `ExecutionProfile`；粮食只保存 Primary、Reasoning、
  Vision、Tool 和恰好一个可选 Fallback 模型引用；
- 第一行初始化永久保底粮、第二行初始化永久常用粮；增加启用状态、自定义粮归档/恢复
  和受保护删除；
- 根据 Provider 清单和 SQLite 观测派生本地性与粮食健康，不保存旧状态；
- 实现单个/多个/全部连接生成范围、保底粮本地优先、最近验证过滤、diff 预览、手工
  调整角色和显式保存；
- 保底粮/常用粮全局可见，只有自定义粮配置用户权限。

验收门：

- 干净数据根只有两份永久系统粮食；
- `nest.db.food_packages` 不含模型能力、上下文、输出或工具权限事实；
- 不可用模型不能被选择或生成；
- 浏览器测试覆盖行顺序、范围生成、五角色、生命周期、权限和动态健康。

关闭或推进：AR-004、AR-005、AR-007、AR-020。

### 阶段四：精灵路由与安全工具执行

范围：

- 精灵页只提供一个主粮字段，候选为用户可见、启用、健康且非保底的粮食；
- 保持 API、CLI、Owner 投影和 Runtime 调用方统一读取数据库粮食仓储；旧
  allowed/fallback 字段和全部 `food_policy.yaml` 读取已经删除；
- 把 `coarse/focus` 改为 Primary/Reasoning/Vision/Tool 语义角色请求；
- 统一解析主粮、可选角色到 Primary、一个可选套餐 Fallback、全局保底和类型化
  `no_available_food`；
- 安全搜索和只读文件同时进入普通与结构化皮层，绑定精灵工作区，并实现超时/条目/
  字节裁剪；
- 代码执行和技能修改在后续隔离契约完成前保持关闭。

验收门：

- 修改主粮无需重建精灵即可生效；
- 结构化与普通生成共用同一角色/回退解析器；
- 端到端测试证明安全工具调用和有界回注；
- 主粮与保底不可用时返回契约规定的精确结果。

关闭或推进：AR-006、AR-008、AR-009、AR-010、AR-011、AR-017。

### 阶段五：契约验收与清理

范围：

- 实现干净临时 `ELFIE_HOME` 的 13 步验收场景；
- 增加 Provider、模型矩阵、粮食和精灵流程的浏览器交互检查；
- 删除断言固定粮食或旧 UI 的过时测试，改为契约测试；
- 运行局部测试、架构测试、完整前后端质量门和敏感信息扫描；
- 每个一致性条目只有拿到验收证据后才能关闭。

验收门：

- 13 步契约全部通过，且没有旧文件或旧回退读取器；
- 一致性台账不存在 open 条目；
- 当前代码和中英文镜像都符合行为契约 1.5；
- 所有权和迁移步骤不重新创建目标 `ai_runtime/` 或
  `infrastructure/ai_runtime/` 模块。

关闭：AR-018 和所有剩余条目。
