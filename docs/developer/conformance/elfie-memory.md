# Elfie Memory conformance

> Status: The source-first Memory baseline, typed production caller path, fresh-store boundary and compatibility cleanup are complete. Retention v2 is the current implementation baseline; the reviewed target is now `memory.v3`, so MEM-012 is open for the half-life/profile policy, bounded archived-state Recall, qualified-outcome reinforcement, durable Outcome/outbox evidence and owner confirmation. The Memory Abstraction Loop remains deferred. No production data migration will be performed before the 0.5 compatibility baseline.<br>
> Baseline: 2026-08-30<br>
> Target: [Elfie Memory design](../designs/elfie-memory-architecture)

This is a temporary conformance register. It records the current gaps against the
Memory design and the evidence required to close them. It does not redefine the
Memory model, authorize schema changes or describe an implementation diary.

## Implementation register

| ID | Severity | Status | Current deviation | Target and closure gate | Evidence / references | Residuals |
| --- | --- | --- | --- | --- | --- | --- |
| MEM-001 | P0 | closed | The old entity/subtype and embedded-edge layout has been replaced for the target Adapter by the reviewed Episode, node, assertion and evidence tables. | Target tables, constraints, ownership and rebuildable lexical projections are implemented without a second edge authority. | target=design §9.1–9.2; inventory=`infrastructure/persistence/memory/{schema.py,sqlite_memory_store.py,sqlite_episode_store.py,sqlite_graph_store.py,sqlite_retrieval_store.py}`; references=persistence scan; verification=target schema/round-trip/reopen tests, affected Memory/Brain/Genesis/adoption tests, architecture tests, Ruff, pycompile and `git diff --check`; residuals=existing legacy files are intentionally unsupported and require explicit backup/rebuild. | None for the current target. |
| MEM-002 | P0 | closed | Closed Episodes now have complete content, source IDs, full-source hashes, idempotency and restart-safe writes; completed interaction candidates and Genesis seeds use the source-first path. Unknown time, occurrence precision, attribution, privacy and projection provenance are explicit. | One validated Episode is the detailed historical source; Memory does not group raw turns. | target=design §3 and §9.1/§9.4; inventory=`sqlite_episode_store.py`, `memory_system.py`, `genesis/initializer.py`; references=Episode and E1 vertical-slice tests; verification=duplicate-submit, content-hash, unknown-time, low-intensity candidate, Genesis source-chain and reopen tests; residuals=upstream event boundary remains the owner of event closure. | None for the target path. |
| MEM-003 | P0 | closed | Nodes, aliases, descriptions, mentions, typed-literal qualified assertions, evidence and many-to-many assertion/evidence links are durable; identity merges retarget history and preserve conflicts. | Independent importance, confidence, polarity, viewpoint, time, typed values and contradiction are retained without blind triple overwrite. | target=design §4 and §9.1–9.3; inventory=`sqlite_graph_store.py`, `schema.py`, `predicates.py`; references=source-first graph tests; verification=alias/cross-Episode resolution, merge retargeting, typed source assertion, predicate rejection, conflict/evidence and projection-diagnostic tests; residuals=historical versions already overwritten by the old database cannot be reconstructed automatically. | Existing legacy data may have unrecoverable conflicting versions. |
| MEM-004 | P0 | closed | Bounded workers claim Episodes, validate grounded model proposals or use the conservative deterministic extractor, then commit a retryable sourced projection in one transaction. A versioned predicate registry, source-hash/revision check and bounded rejection diagnostics are enforced. | Canonical identity, evidence attachment, compatible merge and conflict retention are deterministic; source Episodes survive failures. | target=design §5 and §9.3–9.4; inventory=`elfie/brain/memory/consolidation.py`, `predicates.py`, `sqlite_graph_store.py`; references=source-first worker tests; verification=model grounding, global semantic IDs, retry/lease recovery, predicate/version rejection, projection revision and source-preservation tests; residuals=provider and scheduling policy remain injected/operational choices. | No unbounded model call occurs inside a write transaction. |
| MEM-005 | P0 | closed | `RecallRequest` now performs deterministic Basic/Text candidate search followed by a bounded Local Graph traversal, source lookup, relation/time/facet/privacy filters and declared limits. Active, superseded and conflicting claims retain status and evidence. Common-token prefilters and both graph endpoints are bounded/indexed so the representative large-store latency gate remains predictable. | Text covers rare/unresolved wording; graph traversal covers explicit relationships; sources and conflicts remain visible. | target=design §6 and §9.4; inventory=`sqlite_retrieval_store.py`, `sqlite_graph_store.py`, `schema.py`; references=source-first retrieval tests and the final OPT-003 report; verification=rare-term/alias, person-network, knowledge-object, seed, time-window, positive AND/OR facets, unknown-time, privacy, hop/limit and 10k/50k/200k representative latency checks; residuals=Global/community and vector retrieval remain later projections. | The lexical projection remains rebuildable and does not become a second fact authority. |
| MEM-006 | P0 | closed | `RecallBundle` and its deterministic renderer are implemented; the reasoning Memory reader passes the typed bundle through, and a bounded compiler renders relations, paths, conditions, Episodes, Evidence and conflicts as inert model context. | Upper layers receive bounded nodes, assertions, paths, Episodes, evidence and conflicts through the semantic contract, not raw SQL. | target=design §6 and §9.5; inventory=`memory_records.py`, `recall_renderer.py`, `reasoning/{memory_context.py,memory_compiler.py}`; references=reasoning and renderer tests; verification=stable rendering, hard character cap, lossless typed-bundle round-trip, structured model projection, provenance and no synthetic source for typed nodes; residuals=final natural-language narration remains Reasoning's responsibility. | None for the Memory boundary. |
| MEM-007 | P0 | closed (fresh-store policy) | The importer and legacy source paths have been removed. The Adapter rejects old, mixed or unsupported-version databases before any business write and directs an operator to back up and explicitly rebuild. | Reject old/mixed databases before mutation, leave them unchanged, and create only the current schema after explicit rebuild. | target=design §6.4, §9.5–9.6; inventory=`sqlite_memory_store.py`, `schema.py`; references=ADR-0018 and persistence rules; verification=legacy/mixed/v4 rejection without mutation, fresh-schema creation/reopen, and zero importer references; residuals=none for the development target. | No live user database is touched. |
| MEM-008 | P0 | closed | Deterministic structural gates, the complete real Ark gate and the owner experience review are complete for Stage 1. | A replayable redacted report must show deterministic gates, source grounding, relationship/conflict behavior, restart and latency before Stage 1 promotion. | target=design §9.7 and `docs/.internal/elfie-stage1-memory-backed-chat-execution-plan.md`; inventory=`devtools/evals/stage1_chat_ark.py`, scenario set and focused tests; references=`build/evaluations/stage1-chat/e1-ark-real-final/report.json`; verification=final-candidate report records 86 deterministic E1 tests, 33/33 repeated machine scenarios, 33 structured Ark judge calls with every applicable dimension worst score at least 4, persistence scan exit 0, and the owner-approved anonymized samples; one transient provider empty response was recovered by the existing bounded failure path; residuals=no production Memory migration is planned before the 0.5 compatibility baseline. | Ark authentication and structured judge completion passed; no secret was written to the report. |
| MEM-009 | P0 | closed | Brain, Reasoning and Lab production-facing paths consume typed `RecallBundle`/Memory inspection and the single `Memory Maintenance` entry point. The Lab fallback, old Memory facade branches, old algorithms, old persistence mixins and their focused tests have been removed. | Keep one typed Memory path and prove no old API/class/module references remain. | target=design §§4.2, 4.4–4.5, 6 and 9.5; inventory=`elfie/brain/memory/memory_system.py`, `elfie/brain/reasoning/memory_context.py`, `devtools/elfie_lab/memory_projection.py`, `devtools/evals/opt003_memory_endurance.py`, `infrastructure/persistence/memory`; references=retired-module scan and typed inspection regression; verification=zero old API/class/module references, Memory/Brain/Genesis/Lab/product regressions, architecture tests, Ruff and persistence scan; residuals=none for the current target. | No production caller constructs or imports the retired retrieval or formatting objects. |
| MEM-010 | P0 | closed (maintenance correctness) | Maintenance shares one bounded budget across Consolidation and Lifecycle, schedules fresh Nodes/Assertions after reinforcement, preserves the prior checkpoint on failure, recovers expired leases and fences stale workers by lease owner/attempt. | A retry or competing worker must not skip, duplicate or overwrite a target, and a source Episode must remain retryable after projection failure. | target=design §§4.4, 9.2 and 9.6; inventory=`memory_system.py`, `sqlite_lifecycle_store.py`, `sqlite_graph_store.py`; references=maintenance hardening regressions; verification=one-budget, lifecycle-only wake-up, checkpoint retry, expired-lease recovery, stale-worker fencing and source-preservation tests; residuals=none for maintenance correctness. | No model/network call occurs inside a write transaction. |
| MEM-011 | P0 | closed (v1 baseline; superseded) | The v1 lifecycle safely advances projected Episodes through `full → compressed → digest → archived`, protects sources and scans historical due records, but it also directly decays `importance`. | Preserve the verified lease, restart, source-safety and one-stage transition mechanics; replace its score semantics through MEM-012. | target=historical v1 baseline; inventory=`score_policy.py`, `sqlite_lifecycle_store.py`, `memory_system.py`; references=contract-hardening regressions and `build/evaluations/stage1-chat/opt003-current/report.json`; verification=due historical scan, unprojected-source protection, four-step lifecycle replay, retained digest, importance-only decay and restart checks; residuals=current design compliance is open in MEM-012. | This row is implementation history, not proof of `memory.v3` conformance. |
| MEM-012 | P0 | open (`memory.v3` implementation and closure evidence pending) | Retention v2 is the current code baseline: one `memory.v2` policy owns `I/D/C/F`; Schema v6 persists `importance`/`retention_days` and Node/Assertion-only `confidence`; Evidence is deduplicated by independence; Genesis uses `D=3650`; Recall ranks with `R/I/F/C`; Maintenance uses freshness thresholds without mutating scores; and typed qualified receipts are idempotent. It does not implement the reviewed `retention_profile`/`half_life_days` target, `F=2^(-t/H)`, qualified reinforcement `H'=H*(3-2F)`, the separately capped archived lane or sourced relearning semantics. | Implement one `memory.v3` vertical slice: fresh-store schema and typed API use the versioned `retention_profile`/`half_life_days` admission policy, while Genesis always receives the ten-year profile; Lifecycle alone owns versioned compression/archive/forgetting thresholds; Recall consumes lifecycle state and ranks through `I/F/C`; only durable, qualified outcomes or direct learning events reinforce through the continuous frozen formula, while failed retrieval and authoritative relearning follow their separate rules. Then prove restart-safe Outcome/outbox replay, deterministic vectors and lifecycle/latency gates, rerun the real-model sample and obtain owner confirmation. Keep Pattern/Abstraction deferred. | target=Memory design §§2.3, 3.3–3.4, 4.3, 5–6, 8.4–8.5 and 9.2/9.4; inventory=`memory_records.py`, `score_policy.py`, `memory_store.py`, `memory_system.py`, `schema.py`, SQLite Episode/graph/retrieval/lifecycle stores, Reasoning outcome path, Genesis initializer and OPT-003 evaluator; references=current reviewed design and `docs/.internal/elfie-memory-retention-v2-execution-plan.md`; verification=v2 baseline only: 251 affected Memory/Reasoning/Genesis/Eval tests passed; 223 architecture tests passed; Ruff and mypy passed; persistence scan exited 0; refreshed OPT-003 passed at 10,000 Episodes/50,000 Nodes/200,000 Assertions with Basic p95 59.503ms and Local p95 47.960ms (30 samples); real Ark `arrival-memory` passed deterministic gate, machine gate and judge gate (1 candidate + 1 judge call, all applicable dimensions 5/5) in `/private/tmp/elfie-retention-v2-ark-final/report.json`; focused Retention v2 suite 23/23 passed. | Current code and database remain v2/Schema v6, the Outcome proposal is process-local, and no v3 deterministic, endurance, restart or real-model evidence exists yet. The v2 real-Ark promotion also remains blocked until owner experience confirmation. Fresh schema only; no migration, fallback read or dual write before 0.5. |

## Post-baseline optimization register

The following records the current development priorities after the baseline; it does not rewrite the closed or deferred
status of `MEM-001`–`MEM-008`.

| ID | Priority | Status | Current gap | Next acceptance gate | Evidence / references |
| --- | --- | --- | --- | --- | --- |
| OPT-001 | P0 | closed | The bounded slice compiles the frozen E1 fixture through typed Elfaria World Canon/Genesis, removes duplicated identity facts from the reasoning prompt, verifies publish-failure cleanup, and passes the deterministic E2/E3 gates. | No further OPT-001 implementation gate. | target=OPT-001; inventory=`config/world/elfaria.yaml`, typed Genesis and adoption modules; references=OPT-001 execution brief and E1/E2/E3 scenario set; verification=typed fixture, affected Memory/Reasoning tests, deterministic E2/E3, Ruff and persistence scan passed; residuals=production backfill intentionally not run. |
| OPT-002 | P0 | closed | WorkingContext closes bounded topic Episodes, captures source records before inference, extracts explicitly attributed owner/person facts, preserves aliases and supports explicit correction chains. | The deterministic continuous-learning regression passes all eight source-first scenarios. | target=OPT-002; inventory=`conversation_context.py`, `settlement.py`, `consolidation.py` and SQLite Memory adapter; references=OPT-002 execution brief and Memory design §§9.4–9.5; verification=eight continuous-learning scenarios, affected suite, Ruff and persistence scan passed; residuals=production cutover remains separately governed by MEM-007. |
| OPT-003 | P1 | closed (v2 baseline; `memory.v3` rerun required) | The v2 Lifecycle/Memory Maintenance and endurance evaluator cover representative growth, grounding, restart, retry, lock-wait and Basic/Local latency without a model call. | Preserve the bounded worker/lease controls; after MEM-012 changes schema, ranking or lifecycle semantics, rerun the same scale plus the v3 reinforcement, archived-lane and relearning gates before treating the result as current. | target=design §§6, 8.4–8.5, 9.2 and 9.6; inventory=`sqlite_lifecycle_store.py`, `score_policy.py`, `sqlite_graph_store.py`, `devtools/evals/opt003_memory_endurance.py`; references=`/private/tmp/opt003-v2-full.json` (historical local redacted machine report); verification=v2 baseline: 10,000 Episodes, 50,000 Nodes and 200,000 Assertions; all accepted Assertions grounded; Basic p95 59.503ms and Local p95 47.960ms on 30 repetitions; idempotent retry, restart equality, lock-wait completion, zero retired modules/imports and full→compressed→digest→archived→forgotten replay all passed; seed 513.232s, process duration 537.174s; residuals=v2 had none for its development target, while v3 evidence is open under MEM-012. |
| OPT-004 | P1/P2 | deferred | Real Nest observation, activity and multi-Elfie interaction are outside the current chat loop. | After Stage 2 world integration, validate embodied memory and world-event provenance. | — |
| OPT-005 | P1 | deferred | The graph stores and recalls sourced Nodes/Assertions, but does not yet aggregate them into Pattern knowledge Nodes or apply Patterns through scene-aware recall. `patterns_created` remains zero by design. | Deliver graph-first grouping within Consolidation, grounded model proposal, deterministic validation, Pattern persistence, owner-supplied scene-signature matching/upward traversal, structured `RecallBundle` consumption and outcome feedback as one evaluated vertical slice. | target=design §3.5; inventory=`elfie/brain/memory/consolidation.py`, `memory_records.py`, `reasoning/memory_context.py`; references=Memory Abstraction Loop boundary; verification=future grounding/idempotency tests plus cross-scene recall, application and feedback evaluation; residuals=Pattern generation and application are intentionally absent. |

OPT-001 and OPT-002 were developed in parallel, using separate feature branches and independent evaluations; the
combined regression passed. OPT-003 is closed for the development target with the refreshed v2 evidence above;
OPT-004 and OPT-005 remain deferred.

OPT-001 first-slice evidence (2026-08-28): target=OPT-001 plan §§3–5; inventory=`config/world/elfaria.yaml`,
configuration registry/schema, `elfie/genesis/{contracts.py,initializer.py}`, and
`infrastructure/persistence/elfie_workspace/adoption_profiles.py`; references=Elfaria World Canon/species cards and
typed Genesis tests; verification=the typed fixture compiler test, 15 focused adoption/evaluation tests, the
affected Memory/Reasoning suite, Ruff, `git diff --check`, and the existing persistence scan, with the Canon
containing 42 facts and each published-species adoption selecting 40 eligible knowledge seeds, plus 5 Episodes and
13 private relationship objects; materialization cleanup is now covered by an injected publish-failure test and the
reasoning prompt no longer repeats Selfhood identity facts already supplied by Profile/Canon. The typed `stage1-e1.v2`
fixture passed the deterministic gate and a real Ark single-repetition run (26 provider calls; machine and judge gates
passed) in `/private/tmp/elfie-e1-real-20260828-final2/report.md`. The deterministic E2/E3 gate passed 2 published
species, 96 eligible knowledge queries per species, 240 unknown-boundary queries, and 24 biography combinations
(4 life stages × 3 seeds per species) in `build/evaluations/stage1-chat/opt001-e2e3-final/report.json`. The owner
approved the Stage 1 experience; no production backfill was performed; OPT-002 and OPT-003 are closed for the development target, while OPT-004 remains deferred.

OPT-002 implementation and evaluation evidence (2026-08-28): target=continuous-learning source-first flow and WorkingContext boundary; inventory=`elfie/brain/reasoning/conversation_context.py`, `coordinator.py`, `settlement.py`, `elfie/brain/memory/consolidation.py`, `infrastructure/persistence/memory/{schema.py,sqlite_memory_store.py,sqlite_graph_store.py}`; references=OPT-002 execution brief §3–§7 and Memory design §9.4–§9.5; verification=`devtools/evals/opt002_continuous_learning.py` and `test/devtools/evals/test_opt002_continuous_learning.py` pass all eight scenarios: episode boundaries, entities/aliases/ambiguity, owner correction/restart, conflicts, idempotent replay, failure retry, delivery-failure boundary and Elfie isolation; the combined affected suite is 36/36, Ruff and persistence scan exit 0, report=`build/evaluations/stage1-chat/opt002-final/report.json`; production cutover remains under MEM-007. OPT-003 closure evidence is recorded above.

## Post-acceptance follow-ups

1. Before 0.5, an old Memory database is handled only by explicit backup and fresh-root rebuild; no production-data migration is planned.
2. If the persistent schema or ranking policy changes, rerun the bounded-growth and recoverable-lifecycle evaluation for OPT-003.
3. After Stage 2 world integration, establish the embodied-memory evaluation for OPT-004.
4. Implement OPT-005 only as the complete abstraction-and-application slice defined in design §3.5; do not create unused Pattern data first.
5. Implement and close MEM-012 as the complete `memory.v3` vertical slice before treating the retention, lifecycle and ranking evidence as current.

The required read-only persistence inventory is:

```text
uv run --no-sync python scripts/governance/persistence/scan.py --project-root . --check
```

It must be rerun after schema changes. A row closes only when its target,
inventory, references, verification and residuals are recorded. The reviewed Memory hardening pass and the Retention
v2 implementation remains the current code baseline; `memory.v3`, production cutover, embodied-world evaluation and
the abstraction/application loop remain separately governed.

**Closure state:** open
