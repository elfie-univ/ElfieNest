# Elfie Selfhood conformance

> State: v0.2 structural ownership landed; supported-model behavior and existing-workspace migration remain open<br>
> Baseline: 2026-08-30, `91c26643`<br>
> Target: [Elfie Selfhood and fixed model-header design](../designs/elfie/brain/elfie-selfhood-and-fixed-model-header.md)<br>
> Contracts: [Elfie 2.3](../contracts/elfie.md) and
> [Brain 1.5](../contracts/brain.md)

This temporary register records the remaining source and acceptance gaps after
the phase-1 Selfhood/fixed-header implementation slice. Rows marked closed are
closed for the stated phase-1 scope; open rows still require the evidence listed
in their closure gate and are not implied by structural unit tests.

## Open register

| ID | Severity | Status | Current deviation | Target and closure gate | Evidence / references | Residuals |
| --- | --- | --- | --- | --- | --- | --- |
| SHD-001 | P0 | closed (v0.2 structural) | Runtime Selfhood uses strict `SelfhoodState` with exactly `identity_core` and `adaptive_self`; Genesis validates a complete typed sibling in the creation bundle and separately materializes the Memory self-model node. | Keep the closed two-layer schema and deterministic Genesis co-materialization; reject partial/inconsistent bundles. | `test/elfie/brain/selfhood/test_system.py`, `test/elfie/genesis/test_contracts.py`, and workspace/adoption tests; focused Selfhood/Memory and workspace/Genesis suites pass. |
| SHD-002 | P0 | closed (v0.2 structural) | Ordinary Reasoning receives only `SelfhoodPromptProjection`, missing Selfhood blocks cognition before ModelPort, and the former Profile/Canon observer contract has been removed. Profile-facing views are assembled from the owning stores; no live Canon refresh or Selfhood-owned Profile snapshot remains. | Preserve the runtime no-read/no-fallback behavior and keep Profile projections outside Selfhood ownership. Close with recursive ownership/reference tests and observer behavior evidence showing no live Canon refresh or Brain input. | target=Brain 1.5 Selfhood/Genesis clauses and ADR-0033; inventory=Selfhood contracts/exports, Facade observers, Profile store and all callers; verification=ownership scan, Reasoning tests, Profile boundary tests and observer regressions; residuals=none for the v0.2 structural boundary. |
| SHD-003 | P0 | closed (structural) | Required bundled `ReasoningConstitution` is loaded by Bootstrap; Brain assembles the exact four-block prefix and puts dynamic tools/schema in `TURN_PROTOCOL`. Provider adapters and prompt injectors preserve Brain-owned system content. | Keep exact order, one constitution source, and same-Run byte stability; extend provider-specific checks through the adapter gate when needed. | `config/brain/reasoning-constitution.yaml`, configuration registry/release manifest, `elfie/brain/reasoning/model_header.py`, fixed-header tests, and the no-injector test pass. Provider behavior and full context-window evidence remain separate. |
| SHD-004 | P0 | open | Deterministic projection now renders reviewed natural language and rejects control characters, delimiters and fixed-header injection; raw Big Five values, opaque IDs, evidence and biography are not emitted. | Close only after the deterministic projection fixtures are supplemented by a blinded supported-model behavior matrix. | `elfie/brain/selfhood/system.py`, `SelfhoodPromptProjection`, and fixed-header tests cover projection, injection and no-raw-personality fields. Real-model behavior has not been run in this slice; numeric personality effects remain unverified. |
| SHD-005 | P0 | closed (phase 1) | Per-Elfie `brain/selfhood.yaml` is the only runtime Selfhood seed; generic continuity has no Orientation/Selfhood fields, projections are not persisted, and Memory's authoritative self narrative was deleted. | Preserve one source and fail-closed restart behavior. | `elfie/brain/continuity.py`, `elfie/brain/runtime.py`, `elfie/brain/memory/`, fixed-header tests, and journal-recovery tests cover the boundary; production reference scan is clean. Existing workspaces may still contain old checkpoint/`core_*` data; migration is SHD-007. |
| SHD-006 | P0 | closed (phase 1) | `propose_update`, candidate validation, and commit are sealed with `SelfhoodGrowthDisabledError`/rejection; no Turn, Activity, Emotion, Orientation, or model writer is assembled. | Keep growth disabled until the typed Memory proposal and atomic store are separately approved. | `elfie/brain/selfhood/system.py`, settlement/consolidation wiring, and Selfhood tests cover direct-writer rejection and the no-runtime-writer scan. Growth cadence, algorithm, thresholds, proposal schema, and durable commit remain future work. |
| SHD-007 | P0 | open | Existing resident workspaces can contain the old flat YAML shape, Selfhood journal snapshots and `core_*` Memory nodes. No approved inventory says whether each is rebuilt, transformed, retained as ordinary Memory or deleted. | Before any production rollout, freeze the real-data inventory and approve one recoverable migration/rebuild policy. Close only with backup/rollback, mixed-version rejection, package upgrade and representative real-workspace evidence; no Profile/Canon fallback or silent destructive reset is allowed. | target=design sections 10 and 15; inventory=production resolver, Selfhood YAML, Brain journal checkpoint, Memory `core_*` nodes and all consumers; verification=explicit migration decision plus upgrade/recovery acceptance; residuals=policy intentionally not decided in this phase. | Fresh-development data may be rebuilt only under the repository's existing development-data rules. |

The register is not closure-ready while SHD-004 and SHD-007 remain open. Closed
rows describe the implemented v0.2 structural boundary; they do not authorize silent
migration of existing user workspaces or imply model-behavior quality.
