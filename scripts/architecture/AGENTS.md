# 架构 Scanner 执行规则

本目录实现长期架构契约的机器可证明部分，受根规则、仓库治理契约和
`test/architecture/AGENTS.md` 共同约束。

- Scanner 输出必须稳定、精确到文件与违规点，并映射到契约注册表中的长期契约；仍有
  临时债务时才关联 Conformance ID。禁止宽泛 allowlist、静默忽略或基于当前分支
  自我放行。
- `exact` 用于候选代码与候选 Baseline 精确对账，`subset` 用主分支 Scanner 证明候选
  没有新增债务，`deny-all` 用于基线清零后的永久零容忍状态。
- 修改规则、Registry、分类器或 CI 调用方式属于治理变更，不得混入实现侧文件。
  产品迁移只能删除已有 Baseline 条目，不能改 Scanner 来让实现通过。
- 实现侧按路径作用于全仓并默认封闭：除治理文件和普通说明文档外，其余被跟踪文件均
  属于实现侧，包括产品代码、Developer Tools、普通脚本、根运行配置、非架构测试、
  Manifest、资产、文档站代码和非治理 Workflow；不得为当前违规者建立黑名单。
  治理身份优先，新增目录或未知后缀不能保持未分类。
  治理变更不得修改旧 Baseline，产品迁移只能缩减条目，已有治理契约后禁止新建
  Baseline。
- Scanner、架构测试或治理 CI 变化必须带双语 ADR 更新；Pull Request 与受保护分支
  Push 都要使用基础提交中的不可变 Scanner 检查候选代码。
- 注册表不得保留全 closed Conformance 或空 Baseline；最后一个债务清零后，治理收口
  只允许带完整逐行证据和 ready 标记短暂等待独立治理删除；治理收口必须删除临时产物
  和绑定，并保留 Scanner 的 deny-all 门禁。删除路径必须由基线检查器实际观察。
- 结构清理 Scanner 必须覆盖受契约保护范围的实际目录条目；未知条目失败，开放一致性行
  拥有的临时路径只能缩减，不能新增文件。测试通过不能替代目录盘点、引用追踪和残留报告。
- Scanner 必须同时有正向和违规 Fixture 测试；路径迁移时规则表达所有权和依赖，
  不把临时旧目录写成永久必须存在的目标。
- 有效依赖 Scanner 必须按调用方与目标的所有权矩阵处理可解析的动态模块/脚本目标，
  覆盖 Python、Node、Godot、Shell 和新增源码根的执行表面；不得只扫描 `app/`、只识别 import，或把
  `devtools/` 等某次违规目标写成特判。外部可执行程序不冒充仓库模块依赖。
- 不读取生产数据、不启动服务、不依赖网络；同一输入必须产生同一排序输出和退出码。
