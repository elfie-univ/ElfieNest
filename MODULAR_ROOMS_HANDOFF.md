# 模块化房间工作交接总结

## 1. 交接范围

- 当前分支：`main`
- Godot 项目入口：`godot/project.godot`
- 应用总入口：`godot/main.tscn`
- 总房间场景：`godot/rooms/nest.tscn`
- 总房间生成脚本：`godot/rooms/nest.gd`

这份总结描述当前正式目录。角色位于 `godot/characters/`，房间位于 `godot/rooms/`，二者由 `godot/main.tscn` 在应用层组合。继续修改前先查看 `git status`，不要覆盖尚未提交的场景调整。

## 2. 最终建筑模型

整体是沿走廊直线扩展的建筑：走廊一侧为活动区，另一侧为宿舍；走廊尽头有墙和大型壁画。房间不再手工拖放，而是用统一尺寸和规则动态生成。

### 固定尺寸

| 参数 | 值 |
|---|---:|
| 房间单元间距 `CELL_PITCH` | `5.6 m` |
| 名义净宽 `CLEAR_WIDTH` | `5.5 m` |
| 墙厚 `WALL_THICKNESS` | `0.1 m` |
| 墙高 `WALL_HEIGHT` | `3.0 m` |
| 活动区进深 `ACTIVITY_DEPTH` | `3.7 m` |
| 走廊宽 `CORRIDOR_WIDTH` | `3.0 m` |
| 宿舍进深 `DORM_DEPTH` | `3.9 m` |

### 动态房间数

`房间数 = max(1, ceil(床位数 / 4))`。

- 7 张床：第一间 4 张，第二间只生成前 3 个床位。
- 32 张床：生成 8 间活动房和 8 间宿舍。
- 活动房类型顺序固定为：厨房、聚餐/休息、影音、健身、花园、工作室、音乐室、书房。

### 墙体和颜色

- 每个墙体有实心 `WallCore` 碰撞体，墙面饰层与核心留有可见间隙，避免共面闪烁。
- 活动区使用橙、青、红、绿等明亮主题色；宿舍统一浅灰蓝；走廊和宿舍外侧使用浅色木/奶白方向的颜色。
- 活动区第一、第二个区域（厨房和聚餐/休息区）之间保持开放，不生成共同隔墙；之后的活动区边界生成隔墙。
- 宿舍每个单元都有边界墙；相邻房间不依赖共用一面可变色墙，而是按房间两侧饰面着色。
- 走廊地面为浅色中央大理石带，两侧为深色大理石带，并有分格线/纹理装饰。

## 3. 已落地的核心实现

### 主要脚本

- `godot/rooms/nest.gd`
  - 根据 `bed_count` 动态生成活动房、宿舍、走廊、边界墙、尽头壁画和传送房。
  - 默认演示场景为 32 床、8 房。
  - 运行时支持左键拖拽旋转、右键拖拽平移、滚轮缩放、`R/Home` 重置视角。
- `godot/rooms/activity_room.gd`
  - 按固定顺序直接实例化 `common_area_layouts/` 下的 8 个布局，不使用占位家具。
  - 保留布局场景中已经编排好的根变换、家具尺寸和位置，不在生成器中再次缩放或自动吸墙。
  - 为可见的主要家具生成 AABB 碰撞体；图片、便签和地毯等装饰不生成家具碰撞体。
- `godot/rooms/dorm_room.gd`
  - 保留原始床的旋转、镜像方向和排列顺序。
  - 根据剩余床位只生成实际需要的床；同一排床中心接缝为 0。
  - 床排吸附到分隔墙饰面前 `1 mm`，不通过改变床尺寸解决缝隙。
  - 自动生成入口门框/门、中央地毯、入口对面的壁画和中性灯光。
- `godot/rooms/portal_room.gd`
  - 传送房为 `3 m × 3 m`，三面实体墙，走廊侧有门。
  - 门预留灯带和 `AnimationPlayer`/触发区，便于后续做开门动画。
  - Teleporter、圆形底座发光环和朝门的矩形走道共用同一中心线。
- `godot/rooms/room_geometry.gd`
  - 统一创建地板、墙、饰层、发光体和 AABB 碰撞体。
- `godot/rooms/room_dimensions.gd`
  - 集中保存建筑尺寸和床位/房间计算。
- `godot/rooms/assets/themes/room_palette.gd`
  - 保存活动区域主题配色，不再把主题色写在总房间生成脚本中。

### 关键实现原则

1. `common_area_layouts/` 是公共区域家具尺寸和位置的唯一来源；调整布局时直接编辑对应的 `*_layout.tscn`。
2. `activity_room.gd` 只生成房间壳体、实例化布局和补充主要家具碰撞，不二次改写布局内容。
3. 公共区域顺序由 `ACTIVITY_SCENES` 数组确定，文件名不再使用数字前缀。
4. 碰撞体覆盖墙体和主要家具；书放在书架上、摆件放在桌上等 AABB 交叠可能是预期承载关系，家具审计输出需要结合场景语义判断。

## 4. 家具尺寸与重叠审计

生成场景中的实际 AABB 以 `godot/scripts/test/audit_furniture_layout.gd` 的当前输出为准。该脚本会列出 8 个公共区域的家具尺寸和 AABB 重叠，但重叠记录是检查线索，不自动代表布局错误。吉他包含在 `music_set` 合并网格中，花园 `jardi` 也是合并模型，当前都不能单独移动或测量内部对象。

## 5. 已确认的遗留问题

这些不是本轮应继续猜测的内容，而是交给下一阶段明确处理的事项：

1. **宿舍进深取舍**：现在 `DORM_DEPTH = 3.9 m`。床排贴到分隔墙后，房间外墙方向仍约有两侧各 `7.6 cm` 结构余量。要么把宿舍进深收至约 `3.75 m`，要么保留余量，不要再通过缩放床解决。
2. **花园拆分**：要实现“每株绿植沿指定墙吸附”，必须拆分 `jardi` 资源；继续整体缩放会同时改变花坛和植物比例。
3. **音乐资源拆分**：要让吉他单独可移动/测量，必须把合并 GLB 拆成独立节点或资源。
4. **壁画/相框素材库**：当前可直接复用的独立壁画只有 `godot/rooms/assets/artwork/gallery/img1.jpg`。后续图片继续放入该图库，用资源目录和确定性种子为不同宿舍/相框分配；不要运行时热链网上图片。
5. **交互家具**：椅子、吉他等可移动家具应独立成资源并使用独立碰撞体；床、柜、厨房设备等保持固定。当前实现主要提供静态布局和碰撞，不是完整拖拽系统。
6. **门动画**：传送房已经有触发区和 `AnimationPlayer` 挂点，但开门动画本身尚未制作。

## 6. 验证入口

从仓库根目录执行：

```bash
godot --headless --path godot --script res://scripts/test/test_scene_resource_contract.gd
godot --headless --path godot --script res://scripts/test/test_modular_room_composition.gd
godot --headless --path godot --script res://scripts/test/test_modular_orbit_camera.gd
godot --headless --path godot --script res://scripts/test/audit_furniture_layout.gd
```

已覆盖的关键断言：

- 原始 8 个活动房源场景仍被使用。
- 床的旋转/镜像/排列顺序保留；7 床只生成第二间的 3 张床。
- 32 床生成 8 间活动房和 8 间宿舍。
- 8 个公共区域使用对应布局场景，并保持布局根变换不变。
- 床排墙距、壁画墙距、传送器圆环/走道对齐。
- 墙、家具和主要装饰具有碰撞覆盖。
- 相机旋转、平移、缩放、重置，以及最大布局取景均有测试。

预览输出（若文件仍存在）位于 `docs/room_preview/`；生成脚本为 `godot/scripts/tools/render_modular_room_closeups.gd`。

## 7. 接手顺序

1. 在仓库根目录执行 `git status`，先确认当前未提交变更。
2. 运行上面的三个 Godot 检查，再查看厨房、宿舍、花园、音乐室和工作室近景。
3. 若画面仍有问题，先测最终可见 AABB，再决定“改尺寸”还是“改位置”；不要凭截图整体缩放源房间。
4. 先解决宿舍进深取舍，再拆分花园/音乐资源，最后做壁画素材库和可移动家具交互。
5. 用户确认前不要提交、推送或合并本轮目录重构。
