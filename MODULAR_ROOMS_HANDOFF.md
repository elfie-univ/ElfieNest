# 模块化房间工作交接总结

## 1. 交接范围

- 分支：`wagon-tree`
- 基线提交：`4074797` (`chore: snapshot modular room work on wagon-tree`)
- 独立工作树：`/Users/zhenli/git-code/ElfieNest-wagon-tree`
- Godot 项目入口：`assets/project.godot`
- 主场景：`assets/modular_rooms/modular_nest_demo.tscn`
- 原 `main` 工作区没有被回滚；后续应只在 `wagon-tree` 工作树修改。

这份总结描述的是模块化房间基线。当前 `wagon-tree` 工作树另有未提交的外部变化（`room2/`、`room_bak/` 删除，以及 `compare_rooms.gd` 等比较脚本），这些变化不属于本总结的基线结论，接手时先查看 `git status` 再决定是否保留。

## 2. 最终建筑模型

整体是沿走廊直线扩展的建筑：走廊一侧为活动区，另一侧为宿舍；走廊尽头有墙和大型壁画。房间不再手工拖放，而是用统一尺寸和规则动态生成。

### 固定尺寸

| 参数 | 值 |
|---|---:|
| 房间单元间距 `CELL_PITCH` | `5.6 m` |
| 名义净宽 `CLEAR_WIDTH` | `5.5 m` |
| 墙厚 `WALL_THICKNESS` | `0.1 m` |
| 墙高 `WALL_HEIGHT` | `3.0 m` |
| 活动区进深 `ACTIVITY_DEPTH` | `3.7 m` |
| 走廊宽 `CORRIDOR_WIDTH` | `3.0 m` |
| 宿舍进深 `DORM_DEPTH` | `3.9 m` |

### 动态房间数

`房间数 = max(1, ceil(床位数 / 4))`。

- 7 张床：第一间 4 张，第二间只生成前 3 个床位。
- 32 张床：生成 8 间活动房和 8 间宿舍。
- 活动房类型顺序固定为：厨房、聚餐/休息、影音、健身、花园、工作室、音乐室、书房。

### 墙体和颜色

- 每个墙体有实心 `WallCore` 碰撞体，墙面饰层与核心留有可见间隙，避免共面闪烁。
- 活动区使用橙、青、红、绿等明亮主题色；宿舍统一浅灰蓝；走廊和宿舍外侧使用浅色木/奶白方向的颜色。
- 活动区第一、第二个区域（厨房和聚餐/休息区）之间保持开放，不生成共同隔墙；之后的活动区边界生成隔墙。
- 宿舍每个单元都有边界墙；相邻房间不依赖共用一面可变色墙，而是按房间两侧饰面着色。
- 走廊地面为浅色中央大理石带，两侧为深色大理石带，并有分格线/纹理装饰。

## 3. 已落地的核心实现

### 主要脚本

- `assets/modular_rooms/modular_nest.gd`
  - 根据 `bed_count` 动态生成活动房、宿舍、走廊、边界墙、尽头壁画和传送房。
  - 默认演示场景为 32 床、8 房。
  - 运行时支持左键拖拽旋转、右键拖拽平移、滚轮缩放、`R/Home` 重置视角。
- `assets/modular_rooms/activity_room.gd`
  - 直接实例化原始 8 个活动房场景，不使用占位家具。
  - 隐藏原始房间壳体，只保留家具；用可见 AABB 计算缩放后的真实位置。
  - 先按用途校准家具物理尺寸，再执行墙面吸附，最后生成碰撞体。
  - 厨房冰箱、垃圾箱、柜体分到不同深度带，避免互相插入；两个瑜伽球分开。
  - 工作室窗帘在生成场景中隐藏，因为该房间没有窗户。
- `assets/modular_rooms/dorm_room.gd`
  - 保留原始床的旋转、镜像方向和排列顺序。
  - 根据剩余床位只生成实际需要的床；同一排床中心接缝为 0。
  - 床排吸附到分隔墙饰面前 `1 mm`，不通过改变床尺寸解决缝隙。
  - 自动生成入口门框/门、中央地毯、入口对面的壁画和中性灯光。
- `assets/modular_rooms/portal_room.gd`
  - 传送房为 `3 m × 3 m`，三面实体墙，走廊侧有门。
  - 门预留灯带和 `AnimationPlayer`/触发区，便于后续做开门动画。
  - Teleporter、圆形底座发光环和朝门的矩形走道共用同一中心线。
- `assets/modular_rooms/modular_geometry.gd`
  - 统一创建地板、墙、饰层、发光体和 AABB 碰撞体。
- `assets/modular_rooms/room_dimensions.gd`
  - 集中保存全部建筑尺寸、颜色和床位/房间计算。

### 关键实现原则

1. 不再把整套源房间盲目等比例缩放。原做法会把厨房缩到约 `71.3%`、普通活动区缩到约 `81.8%`，造成冰箱、桌椅、钢琴等偏小且离墙很远。
2. 家具尺寸按可见网格 AABB 和现实使用尺寸分别校准；家具的节点原点不能用于判断墙距。
3. 缩放完成后才吸附固定家具；固定家具目标墙距为 `0.001 m`（约 1 mm），不能使用 10 cm 级肉眼留缝。
4. 只有固定靠墙物件吸附：柜体、书架、电视柜、壁画、墙架等。桌子、椅子、沙发、茶几、地毯、乐器等自由家具不自动吸墙。
5. 碰撞体覆盖墙体和主要家具；书放在书架上、摆件放在桌上等 AABB 交叠是预期承载关系，不应误判成家具穿插。

## 4. 最终家具显示尺寸

以下是生成场景中可见网格的最终 AABB，单位为 `X × Y(高) × Z` 米；模型旋转后 X/Z 可能互换。完整清单见 `docs/房间家具尺寸实测清单.md` 的“最终复测”章节。该文件后面的旧表格是修复前基线，不要拿旧表格判断当前画面。

| 房间 | 最终显示尺寸（重点家具） |
|---|---|
| 厨房 | `Counter 3.000 × 2.598 × 2.000`；冰箱 `0.743 × 2.000 × 1.000`；垃圾箱 `0.400 × 0.800 × 1.250`；`Shelves 2.400 × 1.054 × 2.648`；餐桌 `1.428 × 0.750 × 1.342` |
| 聚餐/休息 | 桌 `1.468 × 0.750 × 2.643`；每把椅 `0.610 × 0.820 × 0.571` |
| 影音 | 沙发 `2.518 × 0.910 × 0.850`；茶几 `1.250 × 0.461 × 0.774`；电视柜 `1.517 × 1.683 × 0.424` |
| 健身 | 每台跑步机 `1.700 × 1.287 × 1.096`；瑜伽球直径约 `0.72`；健身凳 `1.444 × 0.433 × 0.433` |
| 花园 | `jardi 3.637 × 1.698 × 5.474`；这是花坛/绿植合并模型 |
| 工作室 | 普通桌 `1.752 × 0.740 × 0.973`；椅 `0.742 × 0.980 × 0.715`；大型工作台 `3.171 × 1.122 × 2.418`；窗帘不生成 |
| 音乐 | 合并乐器组 `3.000 × 1.580 × 2.650`；鼓组 `1.182 × 0.995 × 1.750`；钢琴 `1.299 × 0.894 × 1.350` |
| 书房 | 书架约 `2.54 × 2.19 × 0.30` 或竖向 `0.28 × 2.23 × 2.58`；桌 `0.940 × 0.740 × 2.539`；椅 `0.586 × 0.820 × 0.549` |
| 宿舍 | 每张双层床 `1.874 × 1.814 × 1.605`；床排中心接缝为 0 |
| 传送房 | Teleporter `1.864 × 1.535 × 2.509`，位于 `3 × 3 m` 房间舞台中心 |

注意：吉他包含在 `music_set` 合并网格中，当前不能逐把测量、移动或单独交互；花园 `jardi` 也为合并模型，不能单独沿墙摆每株植物。

## 5. 已确认的遗留问题

这些不是本轮应继续猜测的内容，而是交给下一阶段明确处理的事项：

1. **宿舍进深取舍**：现在 `DORM_DEPTH = 3.9 m`。床排贴到分隔墙后，房间外墙方向仍约有两侧各 `7.6 cm` 结构余量。要么把宿舍进深收至约 `3.75 m`，要么保留余量，不要再通过缩放床解决。
2. **花园拆分**：要实现“每株绿植沿指定墙吸附”，必须拆分 `jardi` 资源；继续整体缩放会同时改变花坛和植物比例。
3. **音乐资源拆分**：要让吉他单独可移动/测量，必须把合并 GLB 拆成独立节点或资源。
4. **壁画/相框素材库**：当前可直接复用的独立壁画只有 `assets/room/assets/framed_painting/imgs/img1.jpg`。后续应新增 `assets/artwork/`，用资源目录和确定性种子为不同宿舍/相框分配不同图片；不要运行时热链网上图片。
5. **交互家具**：椅子、吉他等可移动家具应独立成资源并使用独立碰撞体；床、柜、厨房设备等保持固定。当前实现主要提供静态布局和碰撞，不是完整拖拽系统。
6. **门动画**：传送房已经有触发区和 `AnimationPlayer` 挂点，但开门动画本身尚未制作。

## 6. 验证入口

从仓库根目录执行：

```bash
godot --headless --path assets -s res://scripts/test_modular_room_composition.gd
godot --headless --path assets -s res://scripts/test_modular_orbit_camera.gd
godot --headless --path assets -s res://scripts/audit_furniture_layout.gd
```

已覆盖的关键断言：

- 原始 8 个活动房源场景仍被使用。
- 床的旋转/镜像/排列顺序保留；7 床只生成第二间的 3 张床。
- 32 床生成 8 间活动房和 8 间宿舍。
- 厨房设备不再与柜体互相穿插；瑜伽球不重叠。
- 活动区墙面锚点、床排墙距、壁画墙距、传送器圆环/走道对齐。
- 墙、家具和主要装饰具有碰撞覆盖。
- 相机旋转、平移、缩放、重置，以及最大布局取景均有测试。

预览输出（若文件仍存在）位于 `docs/room_preview/`；生成脚本为 `assets/scripts/render_modular_room_closeups.gd`。

## 7. 接手顺序

1. 在 `/Users/zhenli/git-code/ElfieNest-wagon-tree` 执行 `git status`，先确认并处理当前未提交的 `room2/room_bak` 删除和比较脚本变化。
2. 运行上面的三个 Godot 检查，再查看厨房、宿舍、花园、音乐室和工作室近景。
3. 若画面仍有问题，先测最终可见 AABB，再决定“改尺寸”还是“改位置”；不要凭截图整体缩放源房间。
4. 先解决宿舍进深取舍，再拆分花园/音乐资源，最后做壁画素材库和可移动家具交互。
5. 不要把未确认的改动合并回 `main`。
