interface:
  display_name: "功能分支 Push 与恢复"
  short_description: "Push one feature branch with bounded Git and gh recovery"
  default_prompt: "使用 $git-submit-and-push 只完成明确授权的功能分支 push 和远端 SHA 核验，不创建 PR。"
